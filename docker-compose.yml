services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: optin
      POSTGRES_PASSWORD: optin
      POSTGRES_DB: optin
    ports:
      - "5433:5432"
    volumes:
      - ./docker/initdb:/docker-entrypoint-initdb.d
